# DeskCam 使用说明

0.6.0：预览截图优先的常用操作区、直接锁焦和自动对焦、保存当前裁剪帧、1080p30、内置快捷键帮助；原有 4K 和高清抓拍保留。升级后请刷新手机 DeskCam 页面。

电脑点击“相机控制”：在可折叠侧栏调整清晰度、相机倍率、对焦方式与焦点位置。倍率和焦距拖动即时应用，忙碌时只保留最新位置；无需点应用。侧栏显示手机返回的实际值和操作结果。按 F1 查看完整说明。默认“纸面清晰 1080p · 15 fps”（目标 12 Mbps），也可选标准 1080p20 / 8 Mbps 或省流 720p20 / 4 Mbps。动态书写可选流畅 1080p30 / 16 Mbps；连续截图可选 4K10 / 20 Mbps，实际手机须支持 3840×2160（竖屏对应 2160×3840）的采集与编码；不支持时恢复原档位。切档会短暂停画、重建编码器，连续切换请等待当前操作完成。设置自动保存到手机。手机端备用控制默认折叠。

整张 A4 / 局部推导：先调好倍率、方向和裁剪，再“保存到”相应预设；“使用”会同时恢复相机参数和电脑画面。相机参数确认成功后才套用电脑裁剪。点“细字检查”查看鼠标所指位置的 100% 源像素，帮助判断合焦；冻结后仍可移动检查。

手动调焦与采集端倍率只在 Safari 提供相应能力时启用，应用后检查实际值；如果未开放，页面明确提示。可点“重新自动对焦”重新打开相机，仍需观察细字是否清晰。iPhone 17e 为单后摄，倍率不代表光学镜头切换，也不保证系统相机的 2× 画质。

Capture HD：已经在 4K 档时直接抓取当前高清画面；其他档位下，手机或电脑点击后，暂时关闭实时编码并请求最高目标 3840×2160 的采集格式，保留已设置的倍率和手动焦距，抓取一张新画面后恢复原实时清晰度。这是高分辨率采集帧抓拍；不是系统 48MP 照片。实际返回尺寸若未超过 1080p，会明确失败，不放大冒充高清。成功后电脑自动打开图片窗口，支持 100% 检查、旋转、复制或保存。它不套用实时视频裁剪。照片暂存内存，本次运行可用“最近一张”重开；不会自动复制。只有“保存照片”才写入选择的位置，“打开保存位置”可打开文件夹；复制只写剪贴板。关闭程序后未保存照片丢失。电脑底部会显示进度及结果，失败明确未生成新照片。

需要系统照片时，在手机点“系统相机拍照”，拍完确认后上传，随后恢复实时画面。系统照片最多保留 1600 万像素，更大照片等比缩小；方向归一化并转为高质量 JPEG。

首次安装：把完整发布包解压到固定目录，双击 Install-DeskCam.cmd，完成一次 Windows 管理员授权。注册后不要单独移动 VirtualCamera 文件夹。

日常打开：双击 Start-DeskCam.cmd。关闭窗口默认缩到托盘；设置里可开启随 Windows 启动。摄像头名称沿用 LocalCam Camera。

首次手机连接：电脑点“连接手机”，选择手机能直接访问的有线校园网 IPv4，扫码。已信任原 LocalCam 证书的手机不必重复安装。已配对时二维码默认为长期入口；只有“首次连接 / 重新配对”生成新的临时凭据。首次配对成功后可添加到主屏幕；此后打开 DeskCam 即可开始，点“停止拍摄”释放摄像头。手机页面必须保持前台。支架模式用黑色遮罩覆盖界面并请求保持屏幕唤醒，可手动调暗亮度。iOS 锁屏或切到其他 App 会暂停相机，回到前台恢复；这不是真正息屏采集。

若反复看到安全警告：下载当前电脑提供的证书，在“VPN 与设备管理”安装后，还要到“关于本机 → 证书信任设置”完全信任 LocalCam Local CA。旧项目可能有同名但不同的证书，连接窗口显示当前 SHA-256 标识。不要每次重新安装；信任正确且 IP 未变时应直接打开。若主屏幕 App 未继承 Safari 配对，先使用 Safari 书签，或在主屏幕内完成一次配对。电脑不能代替用户操作 iPhone 信任开关。

日常建议：放好手机，先调倍率与方向；按 A 重新自动对焦，看细字清晰后按 L 锁焦，再截图。Safari 未开放锁焦能力时按钮会禁用，Space 冻结仅固定电脑画面。

操作：

- A：重新自动对焦；L：锁定焦点 / 恢复自动。锁焦会关闭自动对焦，或用手动模式保持手机报告的当前焦距，检查回读后确认。
- Ctrl+S：把当前裁剪画面或冻结帧保存为 PNG；打开保存对话框前已取帧。
- H：高清抓拍；I：100% 细字检查；P：显示 / 收起相机栏。
- Ctrl+1 / Ctrl+2：使用已保存的整张 A4 / 局部推导预设。
- F1：内置使用流程和快捷键帮助。
- R：顺时针旋转 90°。
- 滚轮：围绕鼠标位置缩放；拖动：平移。
- C：进入裁剪，拖动框选；0：重置裁剪，保留方向。
- Ctrl+C：复制当前处理后画面，去掉输出留边；Ctrl+Alt+C：全局复制。若被其他软件占用，窗口底部会提示，仍可用窗口内 Ctrl+C 或复制按钮。
- Space：冻结/恢复预览与复制，webcam 仍继续实时。点击预览不会解除冻结；旋转、缩放或实际拖动会恢复实时以调整画面。
- F11：纯净模式；Esc 或双击画面退出。
- 置顶按钮：始终在其他窗口上方。
- 方向、裁剪、窗口位置、置顶状态自动保存。

Windows Camera、QQ 或 Chrome 选择 LocalCam Camera。预览不经过虚拟摄像头回读；打开 QQ 不应改变预览内容或源质量。

程序接口仅在本机可访问：

- GET http://127.0.0.1:29100/api/frame.jpg：最新处理后画面，未连接/已过期时返回 503。
- GET http://127.0.0.1:29100/api/status：实际输出新帧率、处理耗时、接收时间及解码方式。
- POST http://127.0.0.1:29100/api/photo/request：请求已连接的手机拍照。
- GET http://127.0.0.1:29100/api/photo.jpg：最近一次完整照片，尚未拍摄时返回 404。
- POST http://127.0.0.1:29100/api/unpair：撤销手机凭据，下次需要重新扫码。

接口不支持浏览器跨域读取。frame.jpg 按请求生成，适合简单脚本/原型；需要高频识别时应接内存帧。

网络变化：电脑 IP 改变后，退出并重新启动 DeskCam，重新生成二维码；不要使用 Tailscale 或 Clash 虚拟网卡 IP。既有证书信任保留。每次启动刷新服务器叶证书，有效期最长一年，且不超过本地 CA 的有效期。

实时视频使用 H.264 / WSS，预览、复制及 frame.jpg 最高 4K；虚拟摄像头保持标准横向 1920×1080 输出，竖向内容等比留边。预览和复制保留源像素，避免竖屏先缩小到 608×1080。没有 JPEG/WebRTC 自动回退或音频。4K 时 webcam 仍为 1080p，无需消费者反复切换格式。Win+Shift+S 只能截取屏幕显示像素；要取得完整 4K 像素请用 Ctrl+C / Ctrl+Alt+C 或 frame.jpg。

验证结果和已知限制以仓库交付记录为准。首次 iPhone 真机、晚高峰网络和 QQ/Chrome 实际使用验收仍需在用户设备上完成，不因 CI 通过而宣称已经验证。
