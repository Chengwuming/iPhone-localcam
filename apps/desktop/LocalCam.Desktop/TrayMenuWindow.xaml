<Window x:Class="LocalCam.Desktop.TrayMenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LocalCam 托盘菜单" Width="224" SizeToContent="Height" WindowStyle="None" ResizeMode="NoResize"
        ShowInTaskbar="False" ShowActivated="True" Topmost="True"
        AllowsTransparency="True" Background="Transparent"
        FontFamily="Segoe UI Variable Text, Segoe UI" Foreground="#242424">
    <Window.Resources>
        <Style x:Key="TrayAction" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#242424" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Padding" Value="13,9" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Surface" Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Surface" Property="Background" Value="#E9E9E9" /></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="Surface" Property="Background" Value="#DDDDDD" /></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter TargetName="Surface" Property="Opacity" Value="0.45" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border Background="#FAFAFA" BorderBrush="#D2D2D2" BorderThickness="1" CornerRadius="9" Padding="6">
        <Border.Effect>
            <DropShadowEffect Color="#55000000" BlurRadius="18" ShadowDepth="4" Opacity="0.45" />
        </Border.Effect>
        <StackPanel>
            <Button Content="显示 LocalCam" Style="{StaticResource TrayAction}" Click="ShowLocalCam_Click" />
            <Button Content="连接 iPhone" Style="{StaticResource TrayAction}" Click="ConnectIphone_Click" />
            <Button Content="设置" Style="{StaticResource TrayAction}" Click="Settings_Click" />
            <Border Height="1" Background="#E1E1E1" Margin="9,5" />
            <Button Content="退出 LocalCam" Style="{StaticResource TrayAction}" Click="Exit_Click" />
        </StackPanel>
    </Border>
</Window>
